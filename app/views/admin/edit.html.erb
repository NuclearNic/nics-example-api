<% content_for :content do %>

  <div class="col-xs-12">
    <h3>Editing <%= controller_name.singularize.capitalize %></h3>
    <%= render 'form' %>
    <%= link_to 'Back', eval("admin_#{controller_name.downcase}_path"), class: 'btn btn-default' %>
  </div>

<% end %>